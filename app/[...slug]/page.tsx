import type { Metadata } from 'next';
import SiteShell from '../components/SiteShell';

const labels:Record<string,string>={makineler:'Zemin Yıkama Makineleri',kiralama:'Makine Kiralama',servis:'Servis ve Bakım','yedek-parca':'Yedek Parça','ikinci-el':'İkinci El Makineler',sektorler:'Sektörel Çözümler',blog:'Bilgi Merkezi','teklif-al':'Teklif Al','makine-secici':'Hangi Makine Bana Uygun?',karsilastir:'Makine Karşılaştırma',iletisim:'İletişim',lp:'Zemin Yıkama Çözümleri'};
export async function generateMetadata({params}:{params:Promise<{slug:string[]}>}):Promise<Metadata>{const {slug}=await params;const title=labels[slug[0]]||slug.at(-1)?.replaceAll('-',' ')||'Sayfa';const path=slug.join('/');return {title:`${title} | Zemin Yıkama Makineleri`,description:`${title} hakkında ihtiyaca göre satış, kiralama ve destek seçeneklerini keşfedin. Teknik ve ticari bilgiler talebe göre doğrulanır.`,alternates:{canonical:`https://www.zeminyikama.com/${path}`},openGraph:{title:`${title} | Zemin Yıkama Makineleri`,description:`${title} için profesyonel zemin temizleme çözümleri.`,url:`https://www.zeminyikama.com/${path}`,type:'website',images:[]},twitter:{card:'summary',title:`${title} | Zemin Yıkama Makineleri`,description:`${title} için profesyonel zemin temizleme çözümleri.`,images:[]}}}
export default async function CatchAll({params}:{params:Promise<{slug:string[]}>}){const {slug}=await params;return <SiteShell slug={slug}/>}
